server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: let React Router handle any path that isn't a real file.
    location / {
        try_files $uri $uri/ /index.html;
    }

    location /actuator/health {
        return 200 "ok";
        add_header Content-Type text/plain;
    }
}
