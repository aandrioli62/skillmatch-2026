# Overrides base/login/messages/messages_it.properties: on SkillMatch, username and
# email always coincide (self-service registration sets the username to the email),
# so the login field is just labeled "Email" instead of the generic "Username o email".
usernameOrEmail=Email
invalidUsernameOrEmailMessage=Email o password non validi.
